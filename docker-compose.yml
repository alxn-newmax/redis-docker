version: "3.8"
services:
  redis:
    container_name: newmax-redis
    image: redis/redis-stack:latest
    ports:
      - 6379:6379
      - 8001:8001
    networks:
      - redis

networks:
  redis:
    name: redis-network
    driver: bridge
